<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MovilBusiness.Controls.ProgressIndicator"
             HorizontalOptions="FillAndExpand"
             VerticalOptions="FillAndExpand"
             BackgroundColor="#B0000000">
  <ContentView.Content>
      <!--<Grid x:Name="gridContainer">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <BoxView Color="Black" Opacity="0.5" Grid.Row="0" HorizontalOptions="Fill" VerticalOptions="Fill" />
            <Frame HasShadow="False" Padding="10,10,10,10" HorizontalOptions="Center" VerticalOptions="Center" Grid.Row="0">
                <ActivityIndicator Color="#1976D2" IsRunning="{Binding IsBusy}" />
            </Frame>
            
        </Grid>-->
        <Frame  HasShadow="False" Padding="10" HorizontalOptions="Center" VerticalOptions="Center">
            <ActivityIndicator Color="#1976D2" IsRunning="{Binding IsBusy}" />
        </Frame>
    </ContentView.Content>
</ContentView>