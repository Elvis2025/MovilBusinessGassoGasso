<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MovilBusiness.Views.Components.TemplateSelector.Reportes.NcfTemplateRow">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <Label Text="{Binding ReDSerie}" FontAttributes="{Binding FontAttribute}"/>
        <Label Text="{Binding RedTipoComprobante}" FontAttributes="{Binding FontAttribute}" Grid.Column="1"/>
        <Label Text="{Binding ReDNCFActual}" FontAttributes="{Binding FontAttribute}" Grid.Column="2"/>
        <Label Text="{Binding ReDNCFMax}" FontAttributes="{Binding FontAttribute}" Grid.Column="3"/>
        <Label Text="{Binding ReDAICF}" FontAttributes="{Binding FontAttribute}" Grid.Column="4"/>
        <Label Text="{Binding ReDFechaVencimiento}" HorizontalOptions="End" Grid.Column="5"/>
    </Grid>
</ContentView>